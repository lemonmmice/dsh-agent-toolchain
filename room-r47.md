# r47（DSH 侧）：把"免检区"再收一刀 —— 被调方读取点，**并如实记下它做不到什么**

## 收的是什么

§38 的边界是"spread 转发工具（`drive({ ...args })`）对幽灵参数闸免检，读点在被调方"。这轮去被调方查：
语料 = **插件目录 + 共享 `lib/`**（薄壳插件的实现就在 lib/ 里：`dsh-verify` → `lib/failure-corpus.mjs`），
测试文件排除；判据 = 参数名有**字段访问**痕迹（`x.P` / `['P']`）。结果 DSH 面 16 个 spread 工具里只剩 **3 个**参数没痕迹（都是"函数参数解构消费"，逐条登记带理由）。

## 判据改了三版，每版都是教训

| 版本 | 现象 | 真因 |
|---|---|---|
| ① 把"像对象字面量的键"也算读点 | **未解析 0（假绿）** | 语料含**工具自己的 schema 声明**（`P: { … }`）⇒ 每个参数都"有读点" |
| ② 只认字段访问 + 只取插件目录 | **22 个假阳性** | 没带**共享 lib/**；没识别**注释里的撇号**（`don't`）⇒ 把注释当字符串，一口吞掉整段代码 |
| ③ 只认字段访问 + 带 lib + 先挖注释 | 剩 3 个 | 解构要么跨不过内层 `{}`/`[]`、要么宽到能在 300 字符窗口里凑出一对括号 ⇒ **又变空断言** |

**经验**：判据放松一点就假绿、收紧一点就假红，"看起来覆盖全"的正则往往什么都没查。
最终只留两条能站住的规则 + 3 条带理由的登记（另有哨兵断言"登记必须仍然必要"，豁免只许缩小）。

## ★★ 证伪结果：**没通过**，我把失败原样记下来

做法：把 `lib/failure-corpus.mjs` 里对 `failure_query.q` 的**真实读取**改掉，看闸认不认。**它没红** ——
因为语料里**别处**也有 `q`（`lib/capture-store.mjs` 的过滤参数同样叫 `q`，且真的被读）。
⇒ 这一关只能证明"**整个语料里这个名字没有任何读取痕迹**"，**不能**证明"**是这条工具**的参数被读了"。
脚本把这次结果记成**证伪未达成**（计入失败项），不许读成"这一关很硬"。

## 下一轮（换成能证明的做法）

① 按"工具 → 被调模块 → **具体函数**"接线，只在该函数体里找读取点（这才能给出"是这条工具的参数被读了"的证明）；
② MCP 面同样接线（本轮**如实未查**，没假装查过）；③ 结构化解构解析（按 depth 收集顶层绑定名）。

## 状态

参数 gate 305 → **309** 条断言全绿；全量 verify **exit 0**；同前：宿主不会热加载。
